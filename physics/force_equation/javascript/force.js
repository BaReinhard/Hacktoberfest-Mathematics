'use strict'

/**
 * Calculates force given mass and acceleration.
 * @param {number} mass
 * @param {number} acceleration
 * @returns {number}
 */
export const force = (mass, acceleration) => {
  return mass * acceleration;
};
