#include <stdio.h>
#include <stdlib.h>
#include <math.h>

double area(base, height){
    double result;
    result = (base*height)/;
    return result;
}

double heron(size1, size2, size3){
    double s;
    double result;
    s = (side1+side2+side3)/2;
    result = sqrt(s*(s-side1)*(s-side2)*(s-side3));
}